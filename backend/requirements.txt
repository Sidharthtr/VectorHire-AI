# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Settings
pydantic==2.10.3
pydantic-settings==2.6.1

# LangGraph / LangChain
langgraph==0.2.55
langchain==0.3.12
langchain-core==0.3.25
langchain-google-genai==2.0.7

# LLM (OpenRouter - OpenAI-compatible)
openai>=1.30.0

# Embeddings
sentence-transformers==3.3.1
torch==2.5.1

# Vector DB
chromadb==0.5.23

# PDF Parsing
PyMuPDF==1.24.14

# Data
numpy==1.26.4

# PDF Generation (for dummy resume script only)
reportlab==4.2.5

# Utilities
python-dotenv==1.0.1

# Hybrid Retrieval
rank-bm25==0.2.2

# Database
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0        # SQLite async driver (local dev)
psycopg2-binary==2.9.10  # PostgreSQL driver (Docker / production)

# Redis cache
redis[hiredis]==5.2.1    # hiredis = fast C parser for Redis protocol

# Evaluation (optional — graceful fallback if not installed)
# pip install ragas deepeval

# Scoring
scikit-learn==1.5.2

# HTTP client (for job ingestion adapters)
httpx==0.27.2

# Fuzzy string matching (near-duplicate job deduplication)
rapidfuzz==3.10.1

# Auth — JWT + bcrypt
# bcrypt is pinned to <4.1 because passlib 1.7.4 has a probe that fails on bcrypt 4.1+
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
